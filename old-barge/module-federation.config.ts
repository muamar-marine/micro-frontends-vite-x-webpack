const config = {
  name: 'old-barge',
  remotes: {},
  exposes: {},
  shared: {
    react: {
      singleton: true,
    },
    'react-dom': {
      singleton: true,
    },
  },
};

export default config;
